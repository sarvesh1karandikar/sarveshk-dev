{
  "name": "sarveshk-dev",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:og": "npx tsx scripts/generate-og.ts",
    "generate:pdf": "npx tsx scripts/generate-pdf.ts",
    "generate": "npm run generate:og && npm run generate:pdf"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "puppeteer-core": "^25.1.0",
    "tsx": "^4.22.4",
    "vite": "^6.0.0"
  }
}
